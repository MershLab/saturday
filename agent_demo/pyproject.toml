[project]
name = "inventory-demo"
version = "0.1.0"
description = "Agentic coding demo: tiny inventory library + CLI"
requires-python = ">=3.10"

[project.scripts]
inventory = "inventory.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
